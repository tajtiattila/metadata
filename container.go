package metadata

/*
type Container struct {
}

func NewContainer(r io.ReadSeeker) (*Container, error) {
	d, err := driver.NewContainer(r)
	if err != nil {
		return err
	}

	const sniffLen = 256
	p := make([]byte, sniffLen)
	n, err := r.Read(p)
	p = p[:n]
	if n == 0 {
		return nil, err
	}
}
*/
